import React from "react";

const Settings = ({ user }) => {
    return (
        <div>
            <h2>Settings</h2>
            {/* Add settings form here */}
        </div>
    );
};

export default Settings;